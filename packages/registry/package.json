{
  "name": "@launchpad/registry",
  "version": "0.1.0-dev.0",
  "description": "Launchpad registry schema, seed entries, and the shared static analyzer.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./registry.json": "./registry.json",
    "./schema.json": "./schema.json"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "validate": "bun src/validate-cli.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.3"
  }
}
