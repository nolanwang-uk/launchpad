# base64-skill

A security test fixture. Contains a base64 literal in one of its
install_commands. The runtime must auto-decode it and show the decoded
form in the diff prompt so the user can see what they're actually
authorizing.
